# 后续计划 (Future Plans)

本文件记录 Agent 的进化状态及待实施的高级功能。

## ✅ 已完成 (Recently Completed)
- [x] **语义向量记忆 (Vector Memory)**: 接入 Qwen `text-embedding-v4`，实现语义搜索，支持长日志**自动分块 (Chunking)** 与 **检索聚合**。
- [x] **插件系统 (Plugin System)**: 实现 [PluginManager](file:///d:/qwen_autogui/core/plugin_manager.py#65-205)，支持 [plugins/](file:///d:/qwen_autogui/core/plugin_manager.py#148-151) 目录热加载与 `/plugins reload` 命令。
- [x] **视觉感知互动模式**: 实现 静默/正常/活泼 三种感知模式，支持 `/mode` 切换。
- [x] **主动寒暄逻辑优化**: 活泼模式下实现“IDE 工作时不打扰，娱乐/空闲时多话”的人性化逻辑。
- [x] **记忆双层架构 (Memory 2.0)**: 事实记忆([memory.jsonl](file:///d:/qwen_autogui/data/memory.jsonl))与主观日记(`data/diary/`)分离，实现每日自我进化与独立日记存储。
- [x] **记忆修剪工具**: 实现 `update_memory` / `delete_memory`，支持向量索引实时同步更新。
- [x] **轻量知识图谱 (Knowledge Graph)**: 实现实体关系三元组提取与 `query_knowledge` 查询。

## 1. 视觉感知与主动性优化
- [ ] **参数可视化配置**: 提供更方便的界面或命令来调节截屏间隔、寒暄冷却时间、日志显示开关。
- [ ] **多模态上下文增强**: 使 Vision 提示词能识别特定的窗口标题和复杂的 UI 报错堆栈。

## 2. 记忆与人格进化系统 (已落地一部分)
- [x] **人格审计与回滚**: 创建 `PERSONA.md` 的版本快照，允许用户通过命令查看人格进化历史并回滚由于“偏见”导致的性格偏差。
- [x] **知识图谱关联**: 探索将长短期记忆转化为图形结构（Graph），使 Agent 能理解“张三是李四的导师”这类逻辑关联。（初步实现）
- [x] **日志自动归档与语义索引**: 每天自动将旧日志向量化，使用户可以搜索“去年冬天我做的那个项目”。（已通过 DiaryManager 实现）

## 3. 自动化能力 (下一阶段重点)
- [ ] **定时任务引擎**: 支持“每分钟监控一次某股票价格”、“每天早上 8 点提醒我日程”等常驻背景任务。
- [ ] **浏览器深度操作 (Playwright/Selenium)**: 从简单的网页抓取升级为真实的浏览器交互（点击、登录、数据爬取）。
- [ ] **文件系统批处理**: 支持“把这文件夹里所有图片都转成 WebP”这种复杂的本地文件操作技能。
- [ ] **办公文档生成与操作**: 实现模型对 PPT、Excel、Word 等 Office 文件的自动生成、读写与操作能力。

## 4. 远程/多端访问
- [ ] **FastAPI 网关**: 为 Agent 建立标准的 API 接口。
- [ ] **Web UI**: 开发一个精美的网页前端，替代命令行交互，支持实时截屏预览。
- [ ] **可视化文件输出管理**: 在 Web UI 中增加模型文件输出目录配置，支持根据文件类型（txt, md, ppt, html, excel 等）规范化存储路径。
- [ ] **浏览器自动化配置参数提取**: 在可视化界面中支持配置浏览器自动化的核心参数（例如指定 Chrome 可执行文件路径、CDP `--remote-debugging-port` 端口号以及 `--user-data-dir` 的自定义路径）。

## 5. 维护与调试
- [ ] **手动重建记忆向量**: 提供 `--rebuild-vectors` 命令或 `/memory rebuild` 指令，用于在手动修改记忆文件或更换 Embedding 模型后，强制刷新全部向量索引。

## 6. 数据存储现代化 (Storage Modernization)
> 当前 sessions、scene_memory、plans、向量索引等均以 JSON/JSONL 平文件存储。随着历史数据积累，检索、事务安全和并发访问方面的局限性会日益凸显。

- [ ] **SQLite 统一存储层**: 将 `sessions`、`scene_memory`、`plans`、`user_profile`、`knowledge_graph` 等所有结构化数据迁移至单一 SQLite 数据库（`data/agent.db`），保留 JSONL 作为可读备份/迁移工具。
  - 好处：支持 SQL 查询、事务、并发安全；`/sessions` 支持按关键词/日期过滤；history 可分页加载，不受文件行数影响。
- [ ] **向量索引迁移至 SQLite-VSS / ChromaDB**: 替换当前的 JSONL 向量存储（`scene_memory_vectors.jsonl`），使用支持 ANN（近似最近邻）的向量数据库，提升大规模语义检索的速度与精度。
  - 候选方案：[sqlite-vec](https://github.com/asg017/sqlite-vec)（无额外进程）、[ChromaDB](https://www.trychroma.com/)（本地模式）、[Qdrant](https://qdrant.tech/)（本地模式）。
- [ ] **数据迁移与兼容脚本**: 提供 `tools/migrate_to_db.py` 脚本，将现有 JSON/JSONL 文件一键导入数据库，支持从旧版本一键升级，无需重新生成记忆或向量。
